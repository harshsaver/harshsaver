{
  "name": "harshsaver-profile",
  "private": true,
  "type": "module",
  "description": "The october.dev hero, rendered as a standalone SVG for the profile README.",
  "scripts": {
    "render": "node scripts/render.mjs",
    "preview": "npm run render && python3 -m http.server 8899"
  },
  "engines": { "node": ">=18" }
}
